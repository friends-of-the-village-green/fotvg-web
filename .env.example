# Copy to .env for local development. Never commit .env.
# Production values are set in the Netlify UI, not here.

# Not secret — safe to expose to the browser
PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production

# SECRET. Viewer scope only — never a write token.
SANITY_API_READ_TOKEN=
